#include <ctype.h>
#include <cs50.h>
#include <stdio.h>
#include <string.h>

int count_letters(string text);

int main(void)
{
    string text1 = get_string("Write text sample: ");
    printf("%s\n", text);
}

int count_letters(string text)
{
    
}