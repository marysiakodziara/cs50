#include <stdio.h>
#include <cs50.h>

int sum(int x);
int sumof = 0;

int main(void)
{
    int x = get_int("number of series: ");
    fib(x);
}

int fib(int x)
{
    if (x > 0)
    {
        x--;
        sumof += ;
        x--;
        sum(x);
    }
    else
    {
        return sumof;
    }

    return 0;

}