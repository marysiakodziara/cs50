#include <stdio.h>
#include <cs50.h>

int reverse(int arr[], int x);

int main(void)
{

}

int reverse(int arr[], int x)
{
    static int i = 0;
    int tmp;

    if (i == x || i = x -1)
    {
        return 0;
    }
    else
    {
        tmp = arr[i];
        arr[x - 1]
    }
}