#include <stdio.h>
#include <cs50.h>

int digit(int x, int i);

int main(void)
{
    int i = 0;
    int x = get_int("number: ");
    digit(x, i);
}

int digit(int x, int i)
{
    static int i
    if (x / 10 < )
}