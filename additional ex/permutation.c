#include <stdio.h>
#include <cs50.h>

void permutation(string word);

int main(void)
{

}

void permutation(string word)
{
    int len = srtlen(word);
    for(int i = 0; i < len; i++)
    {
        
    }
}