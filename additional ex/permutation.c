#include <stdio.h>
#include <cs50.h>

void permutation(string word, int index);
int len;
tmp array[len]

int main(void)
{
    len = srtlen(word);
    for(int i = 0; i < len; i++)
    {
        permutation(word, i);
    }
}

void permutation(string word, int index)
{
    
}