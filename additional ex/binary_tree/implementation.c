#include <stdio.h>
#include <cs50.h>

typedef struct node
{
    int data;
    struct node *left, *right;
} node;

node *create(void);

int main(void)
{

}

node *create(void)
{
    int x;
    node *new_node;
    new_node = (node n) malloc(sizeof(node));

}