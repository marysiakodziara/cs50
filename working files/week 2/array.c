#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string word1 = get_string("give me the word: ");
    int lenght = strlen(word1);
    printf("%i\n", lenght);
    int i = 0;
    for (word1[i]; i < length; i++)
    {
        if()
    }
}